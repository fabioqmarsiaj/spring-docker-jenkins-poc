version: '3.5'
services:
  resource:
    container_name: resource-poc
    build:
      dockerfile: ./Dockerfile
    ports:
      - "${RESOURCE_POC_SERVER_PORT}:${RESOURCE_POC_SERVER_PORT}"
    env_file:
      - .env
    restart: always